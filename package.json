{
	"private": true,
	"name": "asciimath-unicode",
	"version": "0.0.0",
	"author": "Erik Brinkman <erik.brinkman@gmail.com>",
	"license": "MIT",
	"type": "module",
	"scripts": {
		"fmt": "biome format --write && cargo fmt",
		"dev": "bun ./options.html",
		"lint": "tsc && biome check && cargo clippy",
		"export:wasm": "wasm-pack build -t web && cp pkg/convert_bg.wasm .",
		"export:images": "mkdir -p images && <<< $'16\n32\n48\n128' xargs -I@ rsvg-convert -w @ -h @ public/am.svg -o images/am@.png",
		"export:bundle": "bun build src/worker.ts --outfile service_worker.js --minify --define import.meta.url=location.origin && bun build src/convert.ts --outfile convert.js --minify --format=iife && bun build ./options.html --outdir out --sourcemap=linked",
		"export:pack": "zip -r --filesync asciimath-unicode.zip manifest.json images/*.png convert.js service_worker.js convert_bg.wasm out",
		"export": "bun export:images && bun export:wasm && bun export:bundle && bun export:pack",
		"pack": "bun lint && bun test --coverage && bun export"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.0",
		"@emotion/react": "^11.14.0",
		"@emotion/styled": "^11.14.1",
		"@mui/material": "^9.1.1",
		"@types/bun": "^1.3.14",
		"@types/chrome": "^0.1.43",
		"@types/react": "^19.2.17",
		"@types/react-dom": "^19.2.3",
		"react": "^19.2.7",
		"react-dom": "^19.2.7",
		"typescript": "~6.0.3",
		"zod": "^4.4.3"
	}
}
